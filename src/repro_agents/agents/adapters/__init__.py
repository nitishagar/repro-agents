"""Thin adapters mapping agent frameworks onto the core ``Model`` protocol.

The loop state machine lives in the core, so each adapter stays small (<100 lines).
"""

from __future__ import annotations
