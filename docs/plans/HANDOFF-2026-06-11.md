# repro-agents — build state / handoff (2026-06-11)

Repo: **https://github.com/nitishagar/repro-agents** (public, `main`). Local:
`/Users/nagarwal/repos/misc/talk-demo/repro-agents`. Plan:
`docs/plans/2026-06-11-repro-agents-v0.1-pattern-a.md`.

## gh / push protocol (CRITICAL — re-read each session)
- `gh` has TWO accounts; the active one flips to `nagarwal-godaddy` (other sessions).
- Before EVERY push: `gh auth switch --user nitishagar` then `gh api user --jq .login`
  must print `nitishagar`. Remote is HTTPS + local gh credential helper (already set).
- Commit trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Memory file `gh-push-as-nitishagar.md` documents this.

## Inner loop
`make all` = ruff (check+format) + mypy(src) + pytest -m "not integration" + security
(pip-audit [ignored] + `_selfcheck`). Docker oracle tests: `make test-integration`
(needs daemon; `open -a Docker` to start — was up this session). Always run
`uv run ruff format .` before `make all` to avoid format churn.

## DONE & pushed (Phases 0–6, all green; CI incl. Docker job green)
- **0** bootstrap: pyproject (hatchling/src/py≥3.10), Apache-2.0, Makefile, ruff/mypy/
  pre-commit, AGENTS/SECURITY/CONTRIBUTING, CI (matrix 3.10–3.12 + integration job),
  `_selfcheck.py`, public repo created.
- **1** `tools/imports.py` — `collect_imports()` AST inventory.
- **2** `tools/registry.py` — `import_to_distribution`, `PyPIClient(fetch=…)`,
  `verify_names` (anti-slopsquat, fail-closed), `RegistryError`, `NameCheck`,
  `VerificationResult(.verified/.rejected)`.
- **3** `tools/deps.py` — `parse_declared`, `diff`→`DepDiff(undeclared/unused/used)`,
  `run_deptry(runner=…)`→`DeptryResult`, `DeptryUnavailable`, `JsonPayload`.
- **4** `tools/sandbox.py` — `Sandbox` Protocol, `DockerSandbox` (non-root, --read-only,
  --network none on smoke), `LocalSandbox`, `SolveResult/SmokeResult`, `DockerSandbox.available()`.
  `tools/solver.py` — `synthesize_spec`, `prove()`→`Proof` (ok from exit codes; empty spec→skipped ok).
- **5** `report/` (`schema.py` STATUS_*, `Report.to_dict`, `bundle.build_report`/`write_bundle`,
  `render.render_markdown`) + `budget.py` (`Budget`, `BudgetExceeded`). Status derived ONLY
  from oracle. `_selfcheck` tested to catch planted violations.
- **6** `cli.py` (typer: `audit`,`version`; `--no-llm/--sandbox/--python/--out/--deptry`) +
  `agents/envforensics.py::run_environment_forensics(...)`. Fixtures
  `tests/fixtures/{healthy_pkg,faulty_pkg}`. Verified live: healthy→PASS exit0;
  faulty(`six`)→REPAIRABLE, proven in Docker, lock `six==1.17.0`, exit1.
  Exit code: 0 only if status==pass, else 1. ruff excludes tests/fixtures; B008 immune-calls set.

## IN PROGRESS — Phase 7 (agent loop scaffold)
Tests already written: `tests/unit/test_loop.py` (uses `run_loop`, `FakeModel`, `Proposal`,
`ConditionalSandbox`). STILL TO WRITE:
1. `src/repro_agents/agents/models.py`: `@dataclass LoopContext(undeclared_imports, candidate_requirements, last_error="", attempt=0)`,
   `@dataclass Proposal(requirements:list[str], rationale="", input_tokens=0, output_tokens=0)`,
   `@runtime_checkable Model(Protocol).propose(ctx)->Proposal`, `FakeModel(proposals)` (returns
   proposals in order, clamps to last).
2. `src/repro_agents/agents/loop.py`: `run_loop(*, undeclared_imports, model, sandbox, budget=None,
   client=None, python_version="3.12", baseline=None)->LoopResult(proof,proposals,attempts,cost)`.
   Loop: budget.start(); each iter `new_attempt()`+`check_time()` (break on BudgetExceeded);
   `model.propose(ctx)`; `budget.add_tokens(in+out)` (break on exceed); verify proposed names via
   `client.exists` (fail-closed, drop unverifiable); if none safe → record fail + continue;
   else `prove(safe, undeclared_imports, sandbox=…)`; break if proof.ok; else last_error=logs.
   Unbounded guard: if `budget.max_attempts is None and budget.attempts >= 6: break`.
   cost = CostLine(sum in/out tokens). Initial proof = Proof(ok=False, requirements=[], imports_checked=imports).
3. `src/repro_agents/agents/adapters/__init__.py` (docstring) and
   `adapters/smolagents_adapter.py`: module fn `parse_requirements(text)->list[str]` (JSON array
   first via regex `\[.*\]`, else dedup tokens `[A-Za-z0-9][A-Za-z0-9._-]+`); `build_prompt(ctx)`;
   `class SmolagentsModel` (lazy `from smolagents import CodeAgent, LiteLLMModel`;
   `LiteLLMModel(model_id=…, api_key=…or env ANTHROPIC_API_KEY)`; `propose` runs `CodeAgent(tools=[],
   model=…, max_steps=…).run(prompt)`, parse → Proposal; token usage best-effort (0,0)).
4. Wire into `agents/envforensics.py`: add params `model=None, budget=None`; when model is not None,
   call `run_loop(undeclared_imports=smoke_imports, model=…, sandbox=…, budget=…, client=pypi_client,
   baseline=spec)`; use loop.proof/loop.cost; narrative = joined proposal rationales. Else current path.
5. Wire into `cli.py audit`: add `--model` (str|None), `--max-tokens/--max-attempts/--max-seconds`.
   If `--model` and `--no-llm` → BadParameter. If `--model`: build `SmolagentsModel(model_id=model)`
   (try/except → helpful error re ANTHROPIC_API_KEY / [llm] extra), Budget from flags, pass to
   run_environment_forensics. Keep deterministic note when neither.
6. Tests still to write: `tests/unit/test_adapter.py` — `parse_requirements` cases (JSON + plain);
   conformance: `pytest.importorskip("smolagents")`, construct `SmolagentsModel(model_id="anthropic/
   claude-sonnet-4-6", api_key="dummy")`, assert `isinstance(m, Model)`. NO live call.
Then `uv run ruff format .` + `make all` + `make test-integration` (optional) → commit/push.

## PENDING — Phase 8 (docs + GitHub Pages + examples)
- `mkdocs.yml` (Material + mkdocstrings), `docs/{index,usage,architecture,security,design}.md`
  (link the plan), nav. `make docs` (`mkdocs build --strict`) must pass.
- `.github/workflows/docs.yml`: build + deploy to Pages (actions/configure-pages,
  upload-pages-artifact from `site/`, deploy-pages; permissions pages:write,id-token:write).
- Enable Pages: `gh api -X POST repos/nitishagar/repro-agents/pages -f build_type=workflow`
  (or PUT if exists). Verify Pages URL live.
- `examples/walkthrough-01/` (reuse a fixture; show audit + bundle). `benchmarks/repro-gym/README.md`.
- README badges already point at ci.yml/docs.yml/Pages.

## Useful commands
- Demo: `uv run repro-agents audit tests/fixtures/faulty_pkg --no-llm --sandbox docker --no-deptry --out /tmp/x`
- Watch CI: `gh run list --branch main --limit 5`
- Tasks tracker (TaskList) mirrors phases 0–8.
